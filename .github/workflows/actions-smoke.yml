name: Actions Smoke Test

on:
  workflow_dispatch:

jobs:
  smoke:
    name: Runner allocation smoke test
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner starts
        run: echo "GitHub Actions runner allocation OK"
