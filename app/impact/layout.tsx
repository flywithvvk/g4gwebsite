import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Impact",
  description: "Measurable results: 89.5% compliance automation, 3x faster operations, 95% cost savings across India's automobile ecosystem.",
};

export default function ImpactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
